{
  "name": "amira-letter-scoring-cdk",
  "version": "1.0.0",
  "description": "CDK infrastructure for Amira Letter Scoring GPU pipeline",
  "main": "lib/amira-letter-scoring-stack.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "cdk deploy -a 'npx ts-node --prefer-ts-exts bin/amira-letter-scoring.ts'",
    "destroy": "cdk destroy -a 'npx ts-node --prefer-ts-exts bin/amira-letter-scoring.ts'",
    "deploy-parallel": "cdk deploy -a 'npx ts-node --prefer-ts-exts bin/amira-lambda-parallel.ts' AmiraLambdaParallelStack",
    "destroy-parallel": "cdk destroy -a 'npx ts-node --prefer-ts-exts bin/amira-lambda-parallel.ts' AmiraLambdaParallelStack"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "20.6.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "~5.2.2"
  },
  "dependencies": {
    "aws-cdk-lib": "2.212.0",
    "constructs": "10.4.2",
    "source-map-support": "^0.5.21"
  }
}
